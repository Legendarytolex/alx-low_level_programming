#include "main.h"

/**
 * main - a program that prints its name, followed by a new line
 *
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		_putchar("s\n", argv[0]);
	}
	return(0);
}
